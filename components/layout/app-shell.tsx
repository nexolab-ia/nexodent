import type {ReactNode} from "react";import {Logo} from "@/components/brand/logo";const links=[["/dashboard","Resumen"],["/agenda","Agenda"],["/patients","Pacientes"],["/estimates","Presupuestos"],["/billing","Cobros"],["/migration","Migración"]] as const;export function AppShell({children}:{children:ReactNode}){return <div className="app-shell"><aside className="sidebar"><Logo/><nav aria-label="Navegación de la aplicación">{links.map(([href,label])=><a key={href} href={href}>{label}</a>)}</nav></aside><div className="app-content"><header className="topbar"><span className="muted">Avisos de tu clínica</span><a className="button" href="/login">Perfil</a></header>{children}</div></div>}
