export function prepareWhatsApp(input:{phone:string;message:string}):{url:string;editableMessage:string}{const phone=input.phone.replace(/[^0-9]/g,"");if(!phone)throw new Error("Ingresa un teléfono válido.");const editableMessage=input.message.trim();if(!editableMessage)throw new Error("Escribe un mensaje antes de continuar.");return{url:`https://wa.me/${phone}?text=${encodeURIComponent(editableMessage)}`,editableMessage};}
export function unsupportedWhatsAppWorkerOutcome():{outcome:"unsupported";sent:false;reason:string}{return{outcome:"unsupported",sent:false,reason:"WhatsApp automático no está disponible en v1."};}
