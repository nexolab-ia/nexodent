import { foreignKey, index, pgTable, unique, uuid, varchar } from "drizzle-orm/pg-core";
import { clp, createdAt, id, jsonData, organizationId } from "./core";
import { patients } from "./clinical";
import { memberships, organizations, sites } from "./tenant";

export const billingMovements = pgTable("billing_movements", { id: id(), organizationId: organizationId().references(() => organizations.id, { onDelete: "cascade" }), patientId: uuid("patient_id").notNull().references(() => patients.id, { onDelete: "restrict" }), siteId: uuid("site_id").references(() => sites.id, { onDelete: "restrict" }), professionalMembershipId: uuid("professional_membership_id").references(() => memberships.id, { onDelete: "restrict" }), actorMembershipId: uuid("actor_membership_id").notNull().references(() => memberships.id, { onDelete: "restrict" }), kind: varchar("kind", { length: 16 }).notNull(), amountClp: clp("amount_clp"), status: varchar("status", { length: 16 }).notNull().default("posted"), reason: varchar("reason", { length: 500 }).notNull(), evidence: jsonData("evidence"), correctsMovementId: uuid("corrects_movement_id"), createdAt: createdAt() }, (table) => [unique("billing_movements_id_organization_unique").on(table.id, table.organizationId), foreignKey({ columns: [table.patientId, table.organizationId], foreignColumns: [patients.id, patients.organizationId], name: "billing_patient_tenant_fk" }), foreignKey({ columns: [table.siteId, table.organizationId], foreignColumns: [sites.id, sites.organizationId], name: "billing_site_tenant_fk" }), foreignKey({ columns: [table.actorMembershipId, table.organizationId], foreignColumns: [memberships.id, memberships.organizationId], name: "billing_actor_tenant_fk" }), index("billing_movements_patient_idx").on(table.organizationId, table.patientId, table.createdAt), index("billing_movements_collection_idx").on(table.organizationId, table.siteId, table.createdAt)]);
