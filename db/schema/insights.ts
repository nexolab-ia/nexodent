import { boolean, foreignKey, index, pgTable, timestamp, unique, uuid, varchar } from "drizzle-orm/pg-core";
import { createdAt, id, jsonData, organizationId, updatedAt } from "./core";
import { memberships, organizations, sites } from "./tenant";

export const operationalInsights=pgTable("operational_insights",{id:id(),organizationId:organizationId().references(()=>organizations.id,{onDelete:"cascade"}),siteId:uuid("site_id").references(()=>sites.id,{onDelete:"restrict"}),ruleKey:varchar("rule_key",{length:80}).notNull(),ruleVersion:varchar("rule_version",{length:16}).notNull(),evidenceHash:varchar("evidence_hash",{length:64}).notNull(),state:varchar("state",{length:24}).notNull(),fresh:boolean("fresh").notNull(),evidence:jsonData("evidence"),action:jsonData("action"),decisionReason:varchar("decision_reason",{length:500}),decidedByMembershipId:uuid("decided_by_membership_id").references(()=>memberships.id,{onDelete:"restrict"}),decidedAt:timestamp("decided_at",{withTimezone:true}),executedAt:timestamp("executed_at",{withTimezone:true}),createdAt:createdAt(),updatedAt:updatedAt()},table=>[unique("operational_insight_evidence_unique").on(table.organizationId,table.ruleKey,table.ruleVersion,table.evidenceHash),unique("operational_insights_id_organization_unique").on(table.id,table.organizationId),foreignKey({columns:[table.siteId,table.organizationId],foreignColumns:[sites.id,sites.organizationId],name:"insight_site_tenant_fk"}),foreignKey({columns:[table.decidedByMembershipId,table.organizationId],foreignColumns:[memberships.id,memberships.organizationId],name:"insight_decider_tenant_fk"}),index("operational_insights_feed_idx").on(table.organizationId,table.state,table.createdAt)]);
export const insightExclusions=pgTable("insight_exclusions",{id:id(),organizationId:organizationId().references(()=>organizations.id,{onDelete:"cascade"}),category:varchar("category",{length:64}).notNull(),inputHash:varchar("input_hash",{length:64}).notNull(),ruleVersion:varchar("rule_version",{length:16}).notNull(),createdAt:createdAt()},table=>[unique("insight_exclusion_unique").on(table.organizationId,table.category,table.inputHash,table.ruleVersion),index("insight_exclusion_scope_idx").on(table.organizationId,table.createdAt)]);
