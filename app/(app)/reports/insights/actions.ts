"use server";import {revalidatePath} from "next/cache";import {sql} from "@/db/client";import {decideInsight} from "@/features/operational-insights/actions";import {requestTenantContext} from "@/lib/request-context";import {runAsTenant} from "@/lib/tenancy";
export async function reviewInsight(formData:FormData):Promise<void>{const actor=await requestTenantContext();const decision=String(formData.get("decision"));if(decision!=="approve"&&decision!=="discard")throw new Error("Decisión no válida.");await runAsTenant(sql,actor,tx=>decideInsight(tx,actor,{insightId:String(formData.get("insightId")),decision,reason:String(formData.get("reason")??"")}));revalidatePath("/reports/insights");}
