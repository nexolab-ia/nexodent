import {describe,expect,it} from "vitest";import {csvUploadRequest,migrationUploadHeaders,readCsvUploadMetadata} from "@/features/csv-migration/transport";
describe("CSV client/API transport contract",()=>{it("sends the original CSV blob as a raw body with decodable metadata headers",()=>{const file=Object.assign(new Blob(["id,nombre\n1,Ana\n"],{type:"text/csv"}),{name:"pacientes septiembre.csv"});const mapping={sourceKey:"id",firstName:"nombre"};const init=csvUploadRequest(file,"patients",mapping);const headers=new Headers(init.headers);expect(init.method).toBe("POST");expect(init.body).toBe(file);expect(init.body).not.toBeInstanceOf(FormData);expect(headers.get("content-type")).toBe("text/csv");expect(decodeURIComponent(headers.get(migrationUploadHeaders.fileName)!)).toBe(file.name);expect(headers.get(migrationUploadHeaders.sourceType)).toBe("patients");expect(readCsvUploadMetadata(headers)).toEqual({fileName:file.name,sourceType:"patients",mapping});});});
