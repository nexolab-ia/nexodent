import {describe,expect,it} from "vitest";import {NextRequest} from "next/server";import {middleware} from "@/middleware";import {readFile} from "node:fs/promises";
describe("migration routes",()=>{it("protects the migration UI",()=>{expect(middleware(new NextRequest("http://localhost/migration")).status).toBe(307);});it("ships the complete staged UI and blocks import behind preview acceptance",async()=>{const ui=await readFile("app/(app)/migration/migration-client.tsx","utf8"),api=await readFile("app/api/migration/route.ts","utf8");for(const label of ["Archivo","Mapeo","Validación","Vista previa","Importación","Reconciliación"])expect(ui).toContain(label);expect(ui.indexOf('operate("accept")')).toBeLessThan(ui.indexOf('operate("import")'));expect(api).toContain("runAsTenant");expect(ui).toContain("Este lote ya fue aplicado");});});
